# current song
display current song on localhost:3333 so you can use it as a stream overlay

# features
## customizations
- bla bla bla
## import custom css

# install
```bash
cargo build --release
```